// abrir cmd(command prompt) e baixar o prompt-sync com o gerenciador de biblioteca npm : npm install prompt-sync
for (let Contagem10_1 = 10 ; Contagem10_1 > 0 ; Contagem10_1--)
    {
    console.log(Contagem10_1)
}
